! bakefile

out: [../bin/out];
run: [cd src/ && g++];
src: [utils/argparse.c utils/gitpush.c start/quickstart.c commit.cpp exec.cpp main.c];

!main{
    >[run] -o [out] [src];
}

!serve{
    >echo 'done!';
}
